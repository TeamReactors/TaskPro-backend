tags:
  - Board
summary: Get Boards
operationId: getBoards
description: Retrieve all boards.
security:
  - bearerAuth: []
responses: 
  200:
    description: A list of boards.
    content:
      application/json:
        schema:
          $ref: ../../components/schemas/board/getBoards.yaml
  401:
    $ref: ../../components/responses/auth/401.yaml